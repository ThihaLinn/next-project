import { Roboto } from 'next/font/google';
import { Lusitana } from 'next/font/google';

export const inter = Roboto({ weight: '500', subsets: ['latin'] });
export const lusitana = Lusitana({ weight: '700', subsets: ['latin'] });
